# `deep-analysis` 输出结构与分析步骤设计

## 目标

`deep-analysis` 的作用不是把某条新闻写长一点。

它要围绕一个事件，把分散来源整理成一个足够有判断力的答复，让用户快速知道：

1. 这件事到底发生了什么。
2. 现在有哪些主流说法。
3. 哪些说法更可信。
4. 接下来还值不值得继续关注。

如果 `digest` 负责“今天看什么”，那么 `deep-analysis` 负责“这件事到底怎么判断”。
