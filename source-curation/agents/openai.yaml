interface:
  display_name: Source Curation
  short_description: Turn confirmed source intent into provider-neutral sources.json
    updates.
  default_prompt: Refine sources for a configured Skrya topic.
policy:
  allow_implicit_invocation: true
